#Pseudocode
    #Step1. Enter initial velocity (u), 
    #Step2. Enter time (t), 
    #Step3. Enter acceleration (a).
    #Step4. Calculate distance s = (u*t) + (0.5 * a* (t**2)).
    #Step5. print the distance

#Code

u = float(input("Enter initial velocity: "))
time = float(input("Enter time: "))
acceleration = float(input("Enter acceleration: "))

span = (u * t) + (0.5 * a * (t**2))
print("The distance covered is", s)

